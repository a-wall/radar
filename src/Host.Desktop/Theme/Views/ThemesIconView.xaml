<UserControl x:Name="ThemesIcon" x:Class="Host.Desktop.Theme.Views.ThemesIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Host.Desktop.Theme.Views"
             mc:Ignorable="d">
    <Grid Width="15" Height="15">
        <Rectangle Fill="{Binding Foreground, ElementName=ThemesIcon}" Margin="7,2,3,10" SnapsToDevicePixels="True" />
        <Rectangle Fill="{Binding Foreground, ElementName=ThemesIcon}" Margin="4,8,5,2" SnapsToDevicePixels="True" />
        <Rectangle Fill="{Binding Foreground, ElementName=ThemesIcon}" Margin="2,3,10,9" SnapsToDevicePixels="True" />
    </Grid>
</UserControl>
